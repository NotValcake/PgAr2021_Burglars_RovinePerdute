/*public class JumpingVehicle extends Vehicle {

    //FIXME useme Integer.MAX_VALUE  or Double.MAX_VALUE
    public JumpingVehicle(MyMap map) {
        super(map);
    }

    @Override
    public void getMinimumPaths() {
    }


    @Override
    public int[] findQuickestPath(int to) {
        return new int[0];
    }

    private double getDistance( Settlement from, Settlement to){
        return ( Math.abs( from.getHeight() - to.getHeight() ) );
     return 0;
    }

    @Override
    public double getFuel(Settlement from, Settlement to) {
        return 0;
    }
}
*/